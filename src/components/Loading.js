import React from "react";

const Loading = () => {
  return (
    <div className="w-full h-[90vh] flex justify-center items-center ">
      <button className="btn loading btn-outline border-0">loading</button>
    </div>
  );
};

export default Loading;
